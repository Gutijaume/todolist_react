import { createGlobalStyle } from "styled-components";

const GlobalStyle = createGlobalStyle //Creamos unos estilos globales y los expoertamos a app
    `* {
        margin: 0;
        padding: 0;
        font-family: 'Arial';

    }`;

export default GlobalStyle